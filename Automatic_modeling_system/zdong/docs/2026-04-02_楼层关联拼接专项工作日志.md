# 楼层关联拼接专项工作日志

日期：`2026-04-02`

问题：楼层与楼层之间无法进行关联拼接

## 1. 执行动作

- 复查 `storey_inference.py` 的楼层推断逻辑。
- 复查 `pipeline.py` 中 `BimEngine._source_layouts()` 的几何归组方式。
- 对真实 DXF 样本输出显式楼层候选、最终 `source_storey_keys` 和 `storey_layout_trace`。
- 在解析阶段补充 `multi_storey_asset_collapsed` 告警，并新增自动化测试。

## 2. 关键证据

- 真实样本中已识别出显式楼层候选：
  - `1F`
  - `2F`
  - `RF`
- 但最终建模元数据仅输出：
  - `source_storey_keys = ['1F']`
  - `storey_layout_trace = [{'storey_key': '1F', 'source_storey_key': '1F', 'replicated': False}]`

## 3. 处理结果

- 系统现在会在解析阶段显式输出：
  - `multi_storey_asset_collapsed`
- 自动化测试已覆盖“同一资产内出现多楼层标题时必须报警”的场景。
